<!DOCTYPE html>
<html>
<head>
  <title>Twitter</title>
  <%= stylesheet_link_tag 'twitter.css', 'jquery-ui-1.8.16.custom.css' %>:
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag 'jquery-1.4.2.min.js','jquery-ui-1.8.16.custom.min.js', 'application' %>
  <%= csrf_meta_tag %>
  <script>
  jQuery.ajaxSetup({
    'beforeSend': function(xhr){
      xhr.setRequestHeader("Accept", "text/javascript, text/html, application/xml, text/xml, */*")
    } 
  });
  </script>

</head>
<body>
    <div class="r">  
      <% if user_signed_in? %>  
      <%= link_to "HOME",:controller => 'posts', :action => 'index' %>
        Signed in as <%= current_user.email %>. Not you?  
        <%= link_to "Sign out", destroy_user_session_path %>  
        <p> <b> Welcome <%= current_user.user %> </b></p>
     <!--else 
         link_to "Sign up", new_user_registration_path or  
         link_to "Sign in", new_user_session_path  -->
      <%end %>  
    </div>  
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>